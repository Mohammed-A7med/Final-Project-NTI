import joi from 'joi'
import {  generalFields } from '../../middleware/validation.middleware.js'


export const updateBasicProfile = joi.object().keys({
 
}).required()

export const ban = joi.object().keys({
 }).required()

export const updatPassword = joi.object().keys({
 
}).required()